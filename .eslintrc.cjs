module.exports = {
  root: true,
  parser: '@typescript-eslint/parser',
  plugins: [
    '@typescript-eslint',
    'prettier', // Runs Prettier as an ESLint rule
  ],
  extends: [
    'eslint:recommended',
    'plugin:@typescript-eslint/recommended', // Uses the recommended rules from @typescript-eslint/eslint-plugin
    'prettier', // Disables ESLint rules that conflict with Prettier
  ],
  rules: {
    'prettier/prettier': 'warn', // Show Prettier issues as warnings
    // Add any project-specific ESLint rules here
    '@typescript-eslint/no-explicit-any': 'warn', // Warn about explicit 'any'
    '@typescript-eslint/no-unused-vars': ['warn', { argsIgnorePattern: '^_' }], // Warn on unused vars, ignore if prefixed with _
  },
  env: {
    node: true, // For Node.js environment specifics (like module.exports)
    es2021: true, // Use ES2021 globals
  },
  ignorePatterns: [
    'node_modules/',
    'dist/',
    'docs/.vitepress/cache/',
    'docs/api/generated/',
    'coverage/', // Often generated by test runners
  ],
  parserOptions: {
    ecmaVersion: 'latest',
    sourceType: 'module',
  },
};
